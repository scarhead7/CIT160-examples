/* Snell, Dallin
** 09 JAN 2018
** A C code example: ternary conditional.
*/


#include <stdio.h>

int main(void)
{
  (void)printf("%s", (((1 + 1) == 2) ? "Math works!\n" : "Math is broken.\n"));

  return 0;
}
